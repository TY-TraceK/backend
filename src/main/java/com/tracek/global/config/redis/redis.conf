# 외부 접속 허용 (Docker 컨테이너 외부/애플리케이션 접속용)
bind 0.0.0.0

# 기본 포트
port 6379

# 비밀번호 설정
requirepass redissecurepw

# 데이터 영속성 설정 (AOF 활성화: 서버가 꺼져도 데이터 보존)
appendonly yes
appendfilename "appendonly.aof"

# 메모리 관리 (최대 메모리 제한 및 초과 시 최근 사용 안 한 키부터 삭제)
maxmemory 512mb
maxmemory-policy allkeys-lru